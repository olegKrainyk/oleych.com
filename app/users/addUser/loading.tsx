"use server"

import React from 'react'

const Loading: React.FC = () => {
    return (
        <div>
            <div>Add USER</div>
            <div>Loading.....</div>
        </div>
    );
};

export default Loading;
